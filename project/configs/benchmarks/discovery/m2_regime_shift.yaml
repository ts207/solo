id: m2_regime_shift
label: Regime shift on BTC perpetual
symbols:
- BTCUSDT
timeframe: 5m
start: '2022-10-01'
end: '2022-12-01'
search_spec:
  version: 1
  kind: search_spec
  search_mode: subset
  triggers:
    events:
    - VOL_SHOCK
    - TREND_DECELERATION
  contexts: {}
  horizons:
  - 5m
  - 15m
  directions:
  - long
  - short
  entry_lag: 1
  cost_profiles:
  - standard
  expression_templates: '*'
  max_candidates_per_run: 50
event_source: fixture
fixture_event_registry: data/reports/benchmarks/fixtures/m2_regime_shift_event_registry.parquet
negative_control: true
negative_control_reason: "Zero candidates across all modes through multiple fixture rebuilds. FTX collapse period has data gaps or microstructure that suppresses signal generation."
