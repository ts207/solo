event_type: INT_DEPTH_VACUUM_CONFIRM
synthetic_coverage: experimental
description: "DEPTH_COLLAPSE confirmed by LIQUIDITY_VACUUM within 6 bars — depth collapse then liquidity vacuum"
parameters:
  left_event: DEPTH_COLLAPSE
  right_event: LIQUIDITY_VACUUM
  op: confirm
  max_gap_bars: 6
research_family: LIQUIDITY_DISLOCATION
cluster_id: liquidity_stress
