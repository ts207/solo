version: 1
kind: search_spec
metadata:
  phase: benchmark
  description: Narrow maintained benchmark search spec for VOL_SHOCK context comparison.
search_mode: subset
triggers:
  events:
  - VOL_SHOCK
contexts:
  vol_regime: high
horizons:
- 15m
directions:
- long
- short
entry_lag:
- 1
cost_profiles:
- standard
expression_templates:
- continuation
template_policy:
  generic_templates_allowed: true
  reason: "Benchmark spec intentionally uses the abstract continuation family for compatibility/regression coverage; not a promotion search surface."
