version: 1
kind: family_registry
event_families:
  LIQUIDITY_DISLOCATION:
    description: Discrete shocks to order book depth and spread.
    searchable: true
    allowed_templates:
    - liquidity_refill_repair
    - stop_run_repair
    - overshoot_repair
    - only_if_liquidity
    - slippage_aware_filter
  LIQUIDITY_STRESS:
    description: Liquidations and depth depletion during stress regimes.
    searchable: true
    allowed_templates:
    - mean_reversion
    - continuation
    - stop_run_repair
    - overshoot_repair
    - only_if_liquidity
    - slippage_aware_filter
    - only_if_regime
    - only_if_highvol
  VOLATILITY_TRANSITION:
    description: Shifts in realized volatility and range compression.
    searchable: true
    allowed_templates:
    - mean_reversion
    - continuation
    - trend_continuation
    - volatility_expansion_follow
    - pullback_entry
    - only_if_regime
  POSITIONING_EXTREMES:
    description: Extreme funding rates and open interest flushes.
    searchable: true
    allowed_templates:
    - carry_unwind_reversal
    - carry_continuation_confirmed
    - squeeze_followthrough_confirmed
    - convexity_capture
    - only_if_funding
    - only_if_oi
    - tail_risk_avoid
  POSITIONING_UNWIND_DELEVERAGING:
    description: Unwinding of crowded positions and deleveraging waves.
    searchable: true
    allowed_templates:
    - reversal_or_squeeze
    - mean_reversion
    - continuation
    - exhaustion_reversal
    - convexity_capture
    - only_if_funding
    - only_if_oi
    - only_if_regime
    - tail_risk_avoid
  BASIS_FUNDING_DISLOCATION:
    description: Extreme basis and funding rate dislocations.
    searchable: true
    allowed_templates:
    - mean_reversion
    - continuation
    - reversal_or_squeeze
    - carry_unwind_reversal
    - carry_continuation_confirmed
    - basis_funding_convergence
    - basis_convergence
    - convexity_capture
    - only_if_funding
    - only_if_oi
    - only_if_regime
    - only_if_highvol
    - only_if_trend
    - tail_risk_avoid
  FORCED_FLOW_AND_EXHAUSTION:
    description: Exhaustion of trend momentum and climax volume.
    searchable: true
    allowed_templates:
    - forced_flow_rebound
    - cooldown_reversal
    - liquidity_refill_repair
    - momentum_fade
    - range_reversion
    - only_if_trend
    - only_if_regime
    - drawdown_filter
    - mean_reversion
    - exhaustion_reversal
  TREND_FAILURE_EXHAUSTION:
    description: Exhaustion of trend momentum and climax volume reversals.
    searchable: true
    allowed_templates:
    - mean_reversion
    - exhaustion_reversal
    - momentum_fade
    - range_reversion
    - only_if_trend
    - only_if_regime
    - only_if_highvol
    - drawdown_filter
  TREND_STRUCTURE:
    description: Breakouts and trend accelerations.
    searchable: true
    allowed_templates:
    - breakout_followthrough
    - false_breakout_reversal
    - pullback_entry
    - trend_continuation
    - continuation
    - only_if_trend
  STATISTICAL_DISLOCATION:
    description: Mean reversion from statistical z-score extremes.
    searchable: true
    allowed_templates:
    - mean_reversion
    - overshoot_repair
    - tail_risk_avoid
  REGIME_TRANSITION:
    description: Long-term shifts in market regime.
    searchable: true
    allowed_templates:
    - only_if_regime
    - continuation
    - mean_reversion
    - drawdown_filter
    - tail_risk_avoid
  INFORMATION_DESYNC:
    description: Arbitrage opportunities from cross-venue desync.
    searchable: true
    allowed_templates:
    - desync_repair
    - basis_convergence
    - basis_repair
    - lead_lag_follow
    - divergence_continuation
  TEMPORAL_STRUCTURE:
    description: Time-based market structures (Open/Close).
    searchable: true
    allowed_templates:
    - mean_reversion
    - continuation
    - tail_risk_avoid
    - only_if_no_news_window
  EXECUTION_FRICTION:
    description: Slippage and execution friction events.
    searchable: true
    allowed_templates:
    - slippage_aware_filter
    - tail_risk_avoid
state_families:
  LIQUIDITY_DISLOCATION:
    description: Persistent states of low liquidity or high friction.
    searchable: true
  LIQUIDITY_STRESS:
    description: Persistent liquidity stress and depth depletion.
    searchable: true
  VOLATILITY_TRANSITION:
    description: High/Low volatility regimes and post-shock states.
    searchable: true
  POSITIONING_EXTREMES:
    description: Crowded trades and deleveraging regimes.
    searchable: true
  POSITIONING_UNWIND_DELEVERAGING:
    description: Crowded unwinds and deleveraging waves.
    searchable: true
  BASIS_FUNDING_DISLOCATION:
    description: Extreme basis and funding rate dislocations.
    searchable: true
  TREND_STRUCTURE:
    description: Trending vs Choppy regimes.
    searchable: true
  TREND_FAILURE_EXHAUSTION:
    description: Exhaustion of trend momentum and climax volume.
    searchable: true
  STATISTICAL_DISLOCATION:
    description: Stretched or overshot market conditions.
    searchable: true
  REGIME_TRANSITION:
    description: Broad risk-on/risk-off regimes.
    searchable: true
  EXECUTION_FRICTION:
    description: Slippage and friction regimes.
    searchable: true
  TEMPORAL_STRUCTURE:
    description: Time-of-day or session-based states.
    searchable: true
  FORCED_FLOW_AND_EXHAUSTION:
    description: Persistent post-climax and exhaustion states.
    searchable: true
  INFORMATION_DESYNC:
    description: Cross-venue and cross-asset desynchronization states.
    searchable: true
  MICROSTRUCTURE_CONTEXT:
    description: Derived market microstructure context states.
    searchable: true
  REGIME_CONTEXT:
    description: Broad bull, bear, and chop regime context states.
    searchable: true
