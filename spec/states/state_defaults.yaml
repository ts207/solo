version: 1
kind: state_defaults
metadata:
  status: authored
  authored_source: spec/states/state_defaults.yaml
defaults:
  state_scope: source_only
  min_events: 200
  runtime:
    enabled: true
    instrument_classes:
      - crypto
    tags: []
